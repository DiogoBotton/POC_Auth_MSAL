{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "AuthTokenOptions": {
    "Key": "base-chave-chave-chave-chave-chave-chave-chave-chave-chave-chave",
    "Issuer": "poc-auth",
    "Audience": "poc-auth",
    "TokenDuration": 1440
  },
  "AzureAd": {
    "Instance": "https://login.microsoftonline.com/", // URL de login do Azure AD
    "ClientId": "<client-id>", // Substituir pelo ClientId correto (ID do aplicativo)
    "TenantId": "<tenant-id>", // Substituir pelo TenantId correto (ID do locatário)
    "Scope": "API.Auth" // Substituir pelo escopo correto da API
  }
}